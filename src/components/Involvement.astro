---
import type { InvolvementProps } from "../types";

interface Props {
  involvement: InvolvementProps[];
}

const { involvement } = Astro.props;
---

<section id="involvement" class="py-16 bg-black">
  <div class="container mx-auto px-4 max-w-6xl">
    <h2 class="text-3xl font-bold text-center mb-12 text-white">Involvement & Leadership</h2>
    <div class="max-w-4xl mx-auto space-y-8">
      {involvement.map((inv) => (
        <div class="bg-[#1c232d] rounded-lg shadow-md p-6 border-l-4 border-green-500">
          <div class="flex flex-col md:flex-row md:items-center md:justify-between mb-4">
            <div>
              <h3 class="text-xl font-semibold text-white">{inv.role}</h3>
              <p class="text-gray-200 font-medium">{inv.organization}</p>
            </div>
            <div class="text-right mt-2 md:mt-0">
              <p class="text-gray-300">{inv.startDate} - {inv.endDate}</p>
            </div>
          </div>
          <ul class="mt-4 space-y-2">
            {inv.summary.map((item) => (
              <li class="text-gray-300 flex items-start">
                <span class="text-green-400 mr-2 mt-1">•</span>
                <span>{item}</span>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>
</section> 